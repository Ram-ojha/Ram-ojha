import { FilterByIDVValuePipe } from './filter-by-idvvalue.pipe';

describe('FilterByIDVValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIDVValuePipe();
    expect(pipe).toBeTruthy();
  });
});
